---
phase: 03-guardian-n2n3
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/openhuman/guardian/mod.rs
  - src/openhuman/guardian/ops.rs
  - src/openhuman/guardian/schemas.rs
  - src/openhuman/guardian/n2/types.rs
  - src/openhuman/guardian/n3/types.rs
  - src/core/all.rs
  - src/openhuman/config/schema/types.rs
autonomous: true
requirements:
  - GRD-02
  - GRD-03
must_haves:
  truths:
    - "Les parametres N2 (seuils, enabled) sont configurables dans config.toml"
    - "Les parametres N3 (timeout, max_tokens, cache_size) sont configurables dans config.toml"
    - "Les controles RPC N2/N3 sont accessibles pour introspection"
    - "L'initialisation du pipeline complet se fait au demarrage"
    - "Si N2 ou N3 est desactive, le pipeline s'adapte (fail-closed si N3 disabled mais N2 escalade)"
  artifacts:
    - path: "src/openhuman/config/schema/types.rs"
      provides: "GuardianN2Config, GuardianN3Config sections in Config struct"
      min_lines: 20
    - path: "src/openhuman/guardian/schemas.rs"
      provides: "N2/N3/RPC controller schemas (n2_evaluate, n3_status, pipeline_status)"
      min_lines: 60
    - path: "src/openhuman/guardian/ops.rs"
      provides: "N2 evaluate, N3 evaluate, pipeline status operations"
      min_lines: 50
    - path: "src/core/all.rs"
      provides: "N2/N3 controllers registered"
      min_lines: 3
  key_links:
    - from: "config/schema/types.rs"
      to: "guardian/n2/types.rs::N2EngineConfig"
      via: "Config deserialized into N2EngineConfig"
      pattern: "guardian_n2"
    - from: "config/schema/types.rs"
      to: "guardian/n3/types.rs::N3Config"
      via: "Config deserialized into N3Config"
      pattern: "guardian_n3"
    - from: "all.rs"
      to: "guardian::schemas.rs"
      via: "Controllers registered in global registry"
      pattern: "all_guardian_registered_controllers"
---

<objective>
Ajouter la configuration N2/N3 dans le schema config.toml, creer les controleurs RPC pour l'introspection, et assurer l'initialisation du pipeline complet.

**Purpose:** Ce plan finalise l'integation de N2+N3 en rendant leurs parametres configurables (seuils, timeouts, enable/disable), en exposant des RPC pour le debugging et l'observabilite, et enregistrant le tout dans le registre global.

**Output:**
- Sections `[guardian_n2]` et `[guardian_n3]` dans Config
- Controleurs RPC: `guardian.n2_evaluate`, `guardian.n3_status`, `guardian.pipeline_status`
- Ops functions pour N2/N3
- Wiring dans all.rs

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-guardian-n2n3/03-CONTEXT.md

# Patterns de configuration
@src/openhuman/config/schema/types.rs (Config struct — pattern pour ajouter guardian_n2/n3)
@src/openhuman/config/schema/load.rs (Chargement config)

# Patterns controleur existants
@src/openhuman/guardian/schemas.rs (controllers existants)
@src/openhuman/guardian/ops.rs (operations existantes)
@src/core/all.rs (build_registered_controllers)

# Types N2/N3 a referencer
@src/openhuman/guardian/n2/types.rs (N2EngineConfig)
@src/openhuman/guardian/n3/types.rs (N3Config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add N2 and N3 config sections to Config struct</name>
  <files>
    src/openhuman/config/schema/types.rs
    src/openhuman/guardian/n2/types.rs
    src/openhuman/guardian/n3/types.rs
  </files>
  <action>
    **Etape 1:** Dans `src/openhuman/config/schema/types.rs`, ajouter les sections de configuration:

    Trouver la struct `Config` (vers ligne 41) et ajouter les nouveaux champs:

    ```rust
    /// Guardian N2 classifier configuration.
    #[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
    #[serde(default)]
    pub struct GuardianN2Config {
        /// Enable N2 classifier. Default: true.
        pub enabled: bool,
        /// Score threshold for blocking (0.0-1.0). Default: 0.7.
        pub block_threshold: f64,
        /// Score threshold for escalating to N3 (0.0-1.0). Default: 0.3.
        pub escalate_threshold: f64,
        /// Maximum input characters to analyze. Default: 10000.
        pub max_input_chars: usize,
    }

    impl Default for GuardianN2Config {
        fn default() -> Self {
            Self {
                enabled: true,
                block_threshold: 0.7,
                escalate_threshold: 0.3,
                max_input_chars: 10000,
            }
        }
    }

    /// Guardian N3 LLM validator configuration.
    #[derive(Debug, Clone, Serialize, Deserialize, JsonSchema)]
    #[serde(default)]
    pub struct GuardianN3Config {
        /// Enable N3 LLM validator. Default: true.
        pub enabled: bool,
        /// Maximum tokens for LLM response. Default: 256.
        pub max_tokens: u32,
        /// Timeout in milliseconds. Default: 450 (<500ms target).
        pub timeout_ms: u64,
        /// LRU cache size. Default: 100.
        pub cache_size: usize,
        /// Optional model override (uses default if None).
        pub model_override: Option<String>,
    }

    impl Default for GuardianN3Config {
        fn default() -> Self {
            Self {
                enabled: true,
                max_tokens: 256,
                timeout_ms: 450,
                cache_size: 100,
                model_override: None,
            }
        }
    }
    ```

    Ajouter ces champs a la struct `Config`:
    ```rust
    #[serde(default)]
    pub guardian_n2: GuardianN2Config,
    #[serde(default)]
    pub guardian_n3: GuardianN3Config,
    ```

    **Etape 2:** Dans `n2/types.rs`, ajouter une methode pour creer `N2EngineConfig` depuis `GuardianN2Config`:

    ```rust
    impl From<crate::openhuman::config::schema::types::GuardianN2Config> for N2EngineConfig {
        fn from(cfg: crate::openhuman::config::schema::types::GuardianN2Config) -> Self {
            Self {
                block_threshold: cfg.block_threshold,
                escalate_threshold: cfg.escalate_threshold,
                max_input_chars: cfg.max_input_chars,
            }
        }
    }
    ```

    **Etape 3:** Dans `n3/types.rs`, ajouter une conversion similaire pour `N3Config` depuis `GuardianN3Config`.

    **Etape 4:** Ne pas oublier la section `[guardian_n2]` et `[guardian_n3]` dans `app/.env.example` si applicable (optionnel).

    Tests de compilation: `cargo check -p openhuman` (sans les erreurs preexistantes whisper-rs).
  </action>
  <verify>
    <automated>cargo check -p openhuman 2>&1 | grep -E "^error" | head -10 || echo "No errors (expected whisper-rs pre-existing)"</automated>
  </verify>
  <done>
    - GuardianN2Config et GuardianN3Config definis dans le schema config
    - Conversions From<GuardianN*Config> pour N2EngineConfig et N3Config
    - Defaults raisonnables pour les deux sections
  </done>
</task>

<task type="auto">
  <name>Task 2: N2/N3 controller schemas and operations</name>
  <files>
    src/openhuman/guardian/schemas.rs
    src/openhuman/guardian/ops.rs
    src/openhuman/guardian/mod.rs
  </files>
  <action>
    **Etape 1:** Etendre `schemas.rs` avec les nouveaux controles:

    Dans `all_controller_schemas()` et `all_registered_controllers()`, ajouter:

    ```rust
    pub fn all_controller_schemas() -> Vec<ControllerSchema> {
        vec![
            // ... existants (rules_list, rules_reload, evaluate)
            schemas("n2_evaluate"),
            schemas("n3_status"),
            schemas("pipeline_status"),
        ]
    }

    pub fn all_registered_controllers() -> Vec<RegisteredController> {
        vec![
            // ... existants
            RegisteredController { schema: schemas("n2_evaluate"), handler: handle_n2_evaluate },
            RegisteredController { schema: schemas("n3_status"), handler: handle_n3_status },
            RegisteredController { schema: schemas("pipeline_status"), handler: handle_pipeline_status },
        ]
    }
    ```

    Nouveaux schemas (namespace: `guardian`):

    ```rust
    "n2_evaluate" => ControllerSchema {
        namespace: "guardian",
        function: "n2_evaluate",
        description: "Evaluate the N2 classifier for a given tool invocation (debugging).",
        inputs: vec![
            FieldSchema { name: "tool_name", ty: TypeSchema::String, comment: "...", required: true },
            FieldSchema { name: "args", ty: TypeSchema::Json, comment: "...", required: true },
            FieldSchema { name: "command", ty: TypeSchema::String, comment: "...", required: false },
            FieldSchema { name: "file_path", ty: TypeSchema::String, comment: "...", required: false },
        ],
        outputs: vec![
            FieldSchema { name: "result", ty: TypeSchema::Json, comment: "N2Result with scores and decision.", required: true },
        ],
    },
    "n3_status" => ControllerSchema {
        namespace: "guardian",
        function: "n3_status",
        description: "Get N3 validator status (enabled, cache size, last latency).",
        inputs: vec![],
        outputs: vec![
            FieldSchema { name: "status", ty: TypeSchema::Json, comment: "N3 configuration and cache stats.", required: true },
        ],
    },
    "pipeline_status" => ControllerSchema {
        namespace: "guardian",
        function: "pipeline_status",
        description: "Get full pipeline status: N1 rules, N2 scores, N3 verdicts for the last invocation.",
        inputs: vec![],
        outputs: vec![
            FieldSchema { name: "status", ty: TypeSchema::Json, comment: "Pipeline configuration and last result.", required: true },
        ],
    },
    ```

    **Etape 2:** Ajouter les handlers dans `schemas.rs`:

    ```rust
    fn handle_n2_evaluate(params: Map<String, Value>) -> ControllerFuture {
        Box::pin(async {
            log::debug!("[guardian][rpc] n2_evaluate enter");
            let tool_name = params.get("tool_name").and_then(|v| v.as_str()).unwrap_or("unknown");
            let args = params.get("args").cloned().unwrap_or(Value::Null);
            let command = params.get("command").and_then(|v| v.as_str());
            let file_path = params.get("file_path").and_then(|v| v.as_str());
            match crate::openhuman::guardian::ops::n2_evaluate(tool_name, args, command, file_path).await {
                Ok(outcome) => to_json(outcome),
                Err(err) => { log::warn!("[guardian][rpc] n2_evaluate failed: {err}"); Err(err) }
            }
        })
    }

    fn handle_n3_status(_params: Map<String, Value>) -> ControllerFuture {
        Box::pin(async {
            match crate::openhuman::guardian::ops::n3_status().await {
                Ok(outcome) => to_json(outcome),
                Err(err) => { log::warn!("[guardian][rpc] n3_status failed: {err}"); Err(err) }
            }
        })
    }

    fn handle_pipeline_status(_params: Map<String, Value>) -> ControllerFuture {
        Box::pin(async {
            match crate::openhuman::guardian::ops::pipeline_status().await {
                Ok(outcome) => to_json(outcome),
                Err(err) => { log::warn!("[guardian][rpc] pipeline_status failed: {err}"); Err(err) }
            }
        })
    }
    ```

    **Etape 3:** Ajouter les operations dans `ops.rs`:

    ```rust
    /// Evaluate the N2 classifier for debugging purposes.
    pub async fn n2_evaluate(
        tool_name: &str,
        args: Value,
        command: Option<&str>,
        file_path: Option<&str>,
    ) -> Result<RpcOutcome<Value>, String> {
        let config = N2EngineConfig::default();
        let n2 = crate::openhuman::guardian::n2::GuardianN2::new(config);
        let result = n2.evaluate(tool_name, &args, command, file_path);
        let payload = json!({
            "allowed": result.allowed,
            "escalate": result.escalate,
            "scores": result.scores.iter().map(|s| json!({
                "score": s.score,
                "reason": s.reason,
                "triggered_by": s.triggered_by,
            })).collect::<Vec<_>>(),
            "latency_us": result.latency_us,
        });
        Ok(RpcOutcome::new(payload, vec![]))
    }

    /// Get N3 validator status.
    pub async fn n3_status() -> Result<RpcOutcome<Value>, String> {
        let payload = json!({
            "enabled": true, // TODO: lire depuis config globale
            "cache_size": 100,
            "model": "local",
            "version": "1.0",
        });
        Ok(RpcOutcome::new(payload, vec![]))
    }

    /// Get full pipeline status.
    pub async fn pipeline_status() -> Result<RpcOutcome<Value>, String> {
        let n1_rules = get_active_rules().await?;
        let payload = json!({
            "n1": {
                "rules": n1_rules.value,
                "pipeline": "active",
            },
            "n2": {
                "enabled": true,
                "block_threshold": 0.7,
                "escalate_threshold": 0.3,
            },
            "n3": {
                "enabled": true,
                "max_tokens": 256,
                "timeout_ms": 450,
            },
            "pipeline_order": ["n1", "n2", "n3"],
        });
        Ok(RpcOutcome::new(payload, vec![]))
    }
    ```

    **Etape 4:** Dans `mod.rs`, verifier que les nouvelles fonctions sont exportees via `all_guardian_controller_schemas` / `all_guardian_registered_controllers`.
  </action>
  <verify>
    <automated>cargo check -p openhuman 2>&1 | grep -E "^error" | head -10 || echo "Ok"</automated>
  </verify>
  <done>
    - 3 nouveaux controles (n2_evaluate, n3_status, pipeline_status) definis
    - Handlers et ops implementes
    - Schemas suivent le pattern existant (ControllerSchema, FieldSchema, TypeSchema)
  </done>
</task>

<task type="auto">
  <name>Task 3: Register N2/N3 controllers in all.rs and initialization wiring</name>
  <files>
    src/core/all.rs
  </files>
  <action>
    **Etape 1:** Dans `src/core/all.rs`, la section Guardian N1 existante est:

    ```rust
    // Guardian N1 deterministic rules
    controllers.extend(crate::openhuman::guardian::all_guardian_registered_controllers());
    ```

    Les nouveaux controles N2/N3 sont deja inclus dans `all_guardian_registered_controllers()` car on a ajoute les nouveaux schemas a la meme fonction. Donc AUCUN changement n'est necessaire dans `all.rs` — le wiring est automatique.

    Verifier que `all_guardian_registered_controllers()` dans `schemas.rs` inclue bien les 6 controles (3 N1 + 3 N2/N3).

    **Etape 2 (documentation):** Ajouter une note dans le module `guardian/ops.rs` ou `guardian/mod.rs` sur l'ordre d'initialisation:

    ```rust
    /// Ordre d'initialisation du pipeline Guardian complet:
    ///
    /// 1. Au demarrage du core, apres le chargement de la config:
    ///    let n1 = GuardianN1::new(policy, yaml_path);
    ///    let n2 = GuardianN2::new(N2EngineConfig::from(config.guardian_n2));
    ///    let n3 = GuardianN3::new(N3Config::from(config.guardian_n3));
    ///    let pipeline = GuardianPipeline::new(n1, n2, n3);
    ///    GuardianPipeline::init_global(pipeline);
    ///
    /// 2. Dans tool_loop.rs, utiliser GuardianPipeline::try_global() pour evaluer.
    ///
    /// 3. GuardianN1::try_global() est conserve pour compatibilite.
    ///
    /// Les subscribers sont enregistres dans l'event bus au demarrage:
    ///   bus::subscribe_global(Arc::new(GuardianBlockingSubscriber));
    ///   bus::subscribe_global(Arc::new(N2BlockingSubscriber));
    ///   bus::subscribe_global(Arc::new(N3ResultSubscriber));
    ```
  </action>
  <verify>
    <automated>cargo check -p openhuman 2>&1 | grep -E "^error" | head -10 || echo "Ok"</automated>
  </verify>
  <done>
    - 6 controles Guardian enregistres via all_guardian_registered_controllers
    - Documentation de l'ordre d'initialisation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config.toml -> Guardian config | Fichier de configuration local. Modifiable par l'utilisateur. Les seuils sont sous controle utilisateur. |
| RPC controllers -> introspection | Les RPC exposent l'etat interne du Guardian. Information sensible limitée. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | Config.toml thresholds | accept | L'utilisateur peut baisser les seuils (desactiver N2/N3). C'est un choix delibere. Les valeurs par defaut sont securisees. |
| T-03-14 | Information Disclosure | RPC n2_evaluate results | accept | Expose les scores et raisons N2. Utile pour debugging. Pas de secrets. |
| T-03-15 | Tampering | Config defaults | mitigate | Les defaults sont fail-closed: N2 enabled, seuils conservateurs (0.7/0.3), N3 timeout 450ms. |
| T-03-SC | Tampering | Package installs | mitigate | Aucun nouveau package. |
</threat_model>

<verification>
1. `cargo check -p openhuman` — compilation (whisper-rs preexisting ok)
2. Les 6 controles Guardian sont accessibles via RPC
3. La config se charge avec les sections guardian_n2 et guardian_n3
4. Les valeurs par defaut sont fail-closed
</verification>

<success_criteria>
- Sections GuardianN2Config et GuardianN3Config dans Config
- 3 controles RPC supplementaires (n2_evaluate, n3_status, pipeline_status)
- Ops functions implementees
- Wiring dans all.rs (automatique via existant)
- Documentation de l'ordre d'initialisation
</success_criteria>

<output>
Creer `.planning/phases/03-guardian-n2n3/03-04-SUMMARY.md` quand termine
</output>
